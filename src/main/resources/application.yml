server:
  port: 8081

spring:
  applicatoin:
    name: server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://host.docker.internal:8081/eureka

logging:
  level:com:netflix:
    eureka: OFF
    discovery: OFF

